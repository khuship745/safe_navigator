name: safe_navigator
description: "Prevent double-tap navigation bugs in Flutter. A drop-in replacement for Navigator that blocks accidental duplicate pushes, plus a debounced SafeButton for any action."
version: 0.0.1
repository: https://github.com/khuship745/safe_navigator.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
