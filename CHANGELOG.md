## 0.0.1

* Initial release.
* `SafeNavigator` with `push`, `pushNamed`, `pushReplacement`,
  `pushReplacementNamed`, `pushAndRemoveUntil`, `pop`, `maybePop`.
* `SafeButton` widget for debouncing any tap callback.
* Global configurable cooldown via `SafeNavigator.cooldown`.
